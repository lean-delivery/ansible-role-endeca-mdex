---
# Defaut path to installed Endeca MDEX (generated by installer)
mdex_path: "{{ base_root }}/endeca/MDEX/{{ mdex_version }}"

requirements:
  - "unzip"
  - "policycoreutils-python"
  - "libsemanage-python"
  - "libselinux-python"

# required_packages:
#  - unzip
#  - glibc.i686
#  - dos2unix
