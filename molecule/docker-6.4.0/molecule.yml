---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-file: .yamllint
platforms:
  - name: test-endeca-mdex-6.4.0
    image: centos:6
    groups:
      - endeca-mdex
provisioner:
  name: ansible
  config_options:
    ssh_connection:
      control_path: "/var/ans/%%h"
  lint:
    name: ansible-lint
scenario:
  name: docker-6.4.0
verifier:
  name: testinfra
  lint:
    name: flake8
